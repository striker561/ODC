/** Seconds to ease GLB idle out when the user starts interacting. */
export const IDLE_HANDOFF_OUT_SEC = 0.45;

/** Seconds to ease GLB idle back in after interaction ends. */
export const IDLE_HANDOFF_IN_SEC = 0.55;

/** Embedded clip name in `public/hand.glb` (25s loop). */
export const GLB_IDLE_ANIMATION = "Test Animation";
